# Component inventory

Every block-level class in `styles.css`, grouped by the section it is defined in.
Modifiers (`--`) and children (`__`) follow BEM off these blocks, so once you know
**561 block classes across 34 sections.**

the block you can read the rest straight from the stylesheet.

### Banyan OMS — Dashboard theme

`.app`

### Sidebar

`.brand` `.icon-btn` `.nav` `.nav-badge` `.nav-chev` `.nav-dot` `.nav-item` `.nav-sub` `.nav-toggle` `.nav-toggle-wrap` `.side-search` `.side-user` `.sidebar` `.sidebar-backdrop` `.sidebar-close` `.sidebar-foot` `.sidebar-toggle` `.signout` `.staging-badge`

### Main area

`.hamburger` `.main` `.topbar`

### Icon-only (collapsed) sidebar

`.content` `.icon-btn` `.user-chip`

### Page head

`.btn` `.btn-primary` `.page-head` `.page-subtitle` `.page-title`

### Stat cards

`.stat-card` `.stat-icon` `.stat-label` `.stat-note` `.stat-value` `.stats-grid`

### Dashboard grid

`.card` `.dash-grid`

### Calendar

`.cal-body` `.cal-card` `.cal-cell` `.cal-dot` `.cal-dot-more` `.cal-dow` `.cal-event` `.cal-grid` `.cal-head` `.cal-month` `.cal-nav` `.cal-today` `.cal-week` `.kind-checkin` `.kind-checkout` `.kind-hold` `.kind-lease` `.kind-task` `.kind-viewing` `.seg`

### Upcoming panel

`.upcoming`

### Day detail modal

`.modal` `.modal-event` `.modal-overlay`

### Animations

`.cal-card` `.stats-grid`

### Calendar legend

`.cal-legend`

### Availability page

`.avail-body` `.avail-card` `.avail-date` `.avail-empty` `.avail-grid` `.avail-item` `.avail-pill` `.avail-unit` `.crumbs` `.filter-tab` `.filter-tabs`

### Listings page

`.areas-bar` `.avail-pill` `.btn-filter` `.btn-ghost` `.cat-tab` `.cat-tabs` `.info-pill` `.list-badges` `.list-card` `.list-empty` `.list-health` `.list-id` `.list-location` `.list-pagination` `.list-price` `.list-row` `.list-stars` `.list-table` `.list-updated` `.listings-grid` `.listings-table` `.page-count` `.search-field` `.select-field` `.select-wrap` `.sr-only` `.stage-badge` `.status-badge` `.subtle-link` `.toolbar` `.view-link` `.view-toggle`

### Sign in page

`.login-btn` `.login-card` `.login-error` `.login-eye` `.login-field` `.login-form` `.login-input` `.login-input-wrap` `.login-label` `.login-logo` `.login-page` `.login-subtitle` `.login-title`

### Supply Overview page

`.supply-area` `.supply-area-list` `.supply-card` `.supply-detail` `.supply-lower` `.supply-note` `.supply-row` `.supply-section-head` `.supply-section-note` `.supply-section-title` `.supply-summary` `.supply-table` `.supply-table-card` `.supply-table-head` `.supply-table-unit` `.supply-table-wrap` `.supply-tables`

### Client Inbox page

`.inbox-avatar` `.inbox-card` `.inbox-chip` `.inbox-client` `.inbox-date` `.inbox-empty` `.inbox-muted` `.inbox-name` `.inbox-next` `.inbox-objective` `.inbox-phone` `.inbox-summary` `.inbox-table` `.inbox-toolbar`

### Opportunities

`.btn-opp-new` `.opp-action` `.opp-actions` `.opp-actions-head` `.opp-avatar` `.opp-chip` `.opp-client` `.opp-date` `.opp-empty` `.opp-name` `.opp-pager` `.opp-phone` `.opp-table` `.opp-toolbar`

### Shortlists

`.sl-add` `.sl-archive-btn` `.sl-body` `.sl-card` `.sl-curate` `.sl-empty` `.sl-engage` `.sl-fav` `.sl-frow` `.sl-head` `.sl-input` `.sl-interest` `.sl-item` `.sl-layout` `.sl-list` `.sl-live` `.sl-main` `.sl-move` `.sl-new-row` `.sl-note` `.sl-on-item` `.sl-on-list` `.sl-on-tag` `.sl-opp` `.sl-pill` `.sl-remove` `.sl-selector` `.sl-side` `.sl-subtitle` `.sl-tag` `.sl-title`

### Opportunity profile

`.pf-actions` `.pf-check` `.pf-check-grid` `.pf-field` `.pf-foot-link` `.pf-form-head` `.pf-grid2` `.pf-links` `.pf-offers` `.pf-pipeline` `.pf-reco` `.pf-reco-note` `.pf-simple-list` `.pf-textarea` `.pf-timeline` `.pf-viewing`

### Overview redesign (per "update version of overview page.md")

`.attention` `.attention-card` `.attention-grid` `.cal-body` `.cal-filter` `.cal-head` `.cal-l` `.cal-title` `.cal-today-btn` `.cal-toolbar` `.cal-wg` `.dash-calbtn` `.dash-cols` `.dash-date` `.dash-greeting` `.dash-header` `.dash-sub` `.duo-grid` `.panel-head` `.panel-link` `.panel-title` `.section-title` `.upcoming-addr` `.upcoming-card` `.upcoming-cta` `.upcoming-dot` `.upcoming-info` `.upcoming-item` `.upcoming-list` `.upcoming-meta` `.upcoming-name`

### Listing detail page

`.activity-card` `.activity-item` `.activity-list` `.badge` `.card-head` `.detail-actions` `.detail-back` `.detail-btn` `.detail-cols` `.detail-head` `.detail-sub` `.detail-tab` `.detail-tabs` `.detail-title` `.readiness` `.section-title` `.summary-card` `.tasks-card`

### Live Shortlist workspace (shortlist.html)

`.lw-addbtn` `.lw-aibadge` `.lw-archivebtn` `.lw-archivenote` `.lw-archiverow` `.lw-build` `.lw-buildfoot` `.lw-builditem` `.lw-card` `.lw-cardhead` `.lw-clientnote` `.lw-content` `.lw-countpill` `.lw-crumbs` `.lw-customize` `.lw-delbtn` `.lw-drag` `.lw-duo` `.lw-fav` `.lw-favstitle` `.lw-field` `.lw-field-grid` `.lw-filterpill` `.lw-h2` `.lw-headactions` `.lw-headmeta` `.lw-helptext` `.lw-hiddenline` `.lw-label` `.lw-linkactions` `.lw-linkbox` `.lw-linkrow` `.lw-linkstatus` `.lw-livebadge` `.lw-match` `.lw-matchempty` `.lw-matches` `.lw-meta2` `.lw-metric` `.lw-metrics` `.lw-movebtn` `.lw-norequests` `.lw-note` `.lw-num` `.lw-person` `.lw-picker` `.lw-pill` `.lw-pills` `.lw-score` `.lw-sechead` `.lw-sharecard` `.lw-sidecol` `.lw-sortrow` `.lw-sortsel` `.lw-status-pill` `.lw-stepnum` `.lw-support` `.lw-textaction` `.lw-tip` `.lw-title` `.lw-toggle` `.lw-tool` `.lw-toolrow` `.lw-topcard` `.lw-workspace` `.lw-writenote`

### Client feedback (26–27 Aug) — shared form primitives,

`.adv-count` `.btn-filter` `.btn-filters-toggle` `.drawer` `.drawer-backdrop` `.drawer-group` `.drawer-more` `.drawer-row` `.field` `.input-field` `.tag-check` `.tagset` `.toolbar`

### My Work + Relationships (built from client feedback, 26–27 Aug)

`.action-dot` `.board` `.board-col` `.data-table` `.field-hint` `.inline-edit` `.queue-stat` `.queue-stats` `.record-pill` `.rel-activity` `.rel-card` `.rel-fact` `.rel-facts` `.rel-grid` `.rel-id` `.rel-metric` `.rel-type` `.rel-wa` `.status-pill` `.task-card` `.task-chip` `.task-desc-cell` `.task-desc-link` `.task-due` `.work-bar` `.work-filters` `.work-list`

### Opportunities table — client feedback (26–27 Aug)

`.btn-shortlist` `.icon-action` `.opp-client` `.opp-grid` `.opp-name` `.opp-phone-sub` `.th-edit`

### Shortlist Builder — full-width 3-step workflow

`.sb-adv` `.sb-back` `.sb-card` `.sb-cards` `.sb-counter` `.sb-criteria` `.sb-criteria-list` `.sb-crumb` `.sb-generate` `.sb-h2` `.sb-h3` `.sb-h4` `.sb-hint` `.sb-internal` `.sb-matchcount` `.sb-newbadge` `.sb-newpill` `.sb-publish` `.sb-published` `.sb-rejected` `.sb-status` `.sb-step` `.sb-steps` `.sb-tag` `.sb-undo` `.sb-url`

### Client-facing shortlist — mobile first

`.cs-act` `.cs-attached` `.cs-back` `.cs-card` `.cs-favbar` `.cs-foot` `.cs-heart` `.cs-hero` `.cs-hidden` `.cs-intro` `.cs-list` `.cs-logo` `.cs-main` `.cs-mini` `.cs-msgbtn` `.cs-page` `.cs-share` `.cs-title` `.cs-toast` `.cs-top` `.cs-who`

### Property detail page

`.cs-backfoot` `.cs-body` `.cs-fav` `.cs-feature` `.cs-featuregrid` `.cs-gallery` `.cs-h2` `.cs-note` `.cs-notes` `.cs-spec`

### Opportunity page — header, tabs, panels

`.btn-whatsapp` `.op-actionmenu` `.op-actionstatus` `.op-block` `.op-fact` `.op-grid` `.op-h2` `.op-head` `.op-list` `.op-listitem` `.op-panel` `.op-tab` `.op-tabs` `.op-tagblock` `.op-timeline` `.op-tl` `.op-when`

### Listing detail — status header, Content / Media / Operations

`.detail-col` `.detail-cols` `.detail-panel` `.detail-side` `.detail-status` `.ld-expand` `.ld-gallery` `.ld-more` `.ld-stars` `.ld-thumb`

### Relationship detail — a workspace, not a database dump

`.op-when` `.rd-assigned` `.rd-broker` `.rd-cols` `.rd-deeper` `.rd-fact` `.rd-facts` `.rd-head` `.rd-kv` `.rd-kvrow` `.rd-mail` `.rd-main` `.rd-noterow` `.rd-side`

### Reporting · Marketing · Settings

`.bl-star` `.queue-stat` `.rp-card` `.rp-custom` `.rp-down` `.rp-flat` `.rp-funnel` `.rp-kpi` `.rp-kpirow` `.rp-live` `.rp-muted` `.rp-num` `.rp-soon` `.rp-sooncard` `.rp-soonpill` `.rp-syncpill` `.rp-up` `.sc-card` `.sc-grid` `.sc-msg` `.sc-qactions` `.sc-review` `.sc-state` `.seg` `.wd-frame` `.ws-imgurl` `.ws-mono` `.ws-slider` `.ws-sliders` `.ws-slug` `.ws-textgroup`

### Component fit — nothing clipped, nothing wrapped mid-label

`.board` `.btn` `.btn-shortlist` `.cal-body` `.cal-card` `.cal-head` `.cal-title` `.cal-toolbar` `.card` `.cat-tab` `.cat-tabs` `.content` `.cs-list` `.detail-panel` `.detail-tabs` `.drawer-row` `.field` `.inbox-toolbar` `.listings-table` `.main` `.op-block` `.op-grid` `.op-panel` `.op-tab` `.op-tabs` `.opp-table` `.opp-toolbar` `.panel-head` `.queue-stats` `.rel-grid` `.rp-funnel` `.sb-alllocs` `.sb-card` `.sb-cards` `.sb-criteria` `.sb-listening` `.sb-liveline` `.sb-locgroup` `.sb-locgroups` `.sb-locsearch` `.sb-newflag` `.sb-selfilter` `.sb-selstat` `.sb-selstats` `.sb-state` `.sb-step` `.sb-steps` `.sb-tool` `.sb-warn` `.sc-grid` `.select-field` `.toolbar` `.view-link` `.work-bar` `.work-filters` `.work-list`
